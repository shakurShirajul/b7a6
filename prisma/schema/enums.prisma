enum Role {
    DONOR
    PATIENT
    ADMIN
}


enum USER_STATUS {
    ACTIVE
    INACTIVE
    PENDING
}

enum NotificationType {
  BLOOD_REQUEST
  REQUEST_ACCEPTED
  REQUEST_REJECTED
  DONATION_COMPLETED
  SYSTEM
}